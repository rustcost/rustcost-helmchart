apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustcost-core.fullname" . }}
  labels:
    {{- include "rustcost-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rustcost-core.selectorLabels" . | nindent 4 }}
