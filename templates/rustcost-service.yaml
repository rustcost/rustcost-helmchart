apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustcost.fullname" . }}
  labels:
    {{- include "rustcost.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rustcost.selectorLabels" . | nindent 4 }}
