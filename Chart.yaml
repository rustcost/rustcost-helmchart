apiVersion: v2
name: rustcost
description: A Helm chart for deploying Rustcost (Axum backend, optional React frontend, optional PostgreSQL, and node-level metrics collectors like node-exporter and cAdvisor)

type: application

# Chart version (increment this when you change templates)
version: 1.0.0-dev.2

# App version (matches your Rustcost release, or "latest" if dev)
appVersion: "1.0.0-dev.2"

keywords:
  - rust
  - axum
  - monitoring
  - node-exporter

sources:
  - https://github.com/rustcost/rustcost-helmchart

dependencies: []
